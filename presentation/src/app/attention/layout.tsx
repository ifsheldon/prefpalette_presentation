export const metadata = {
  title: "Attention-based Preference Modeling — PrefPalette",
};

export default function AttentionLayout(props: { children: React.ReactNode }) {
  return props.children;
}


