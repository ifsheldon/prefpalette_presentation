export const metadata = {
  title: "Counterfactual Attribute Synthesis — PrefPalette",
};

export default function CounterfactualLayout(props: { children: React.ReactNode }) {
  return props.children;
}


